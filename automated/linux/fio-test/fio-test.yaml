metadata:
    name: fio-test
    format: "Lava-Test-Shell Test Definition 1.0"
    description: "FIO or Flexible IO is a versatile IO workload generator test
                  on Linux."
    maintainer:
        - naresh.kamboju@linaro.org
        - chase.qi@linaro.org
    os:
        - debian
        - ubuntu
        - centos
        - fedora
    devices:
        - mustang
        - overdrive
        - d02
        - d03
        - hi6220-hikey
        - apq8016-sbc
    scope:
        - performance
    environment:
        - lava-test-shell

params:
    # The currenty working directory will be used by default. Use '-p' specify
    # block device partition that used for fio test. Example: /dev/sdb1
    # Use disk-partitioning.yaml to create and format partition.
    PARTITION: ""
    # Available IO engines: sync, psync, libaio 
    IOENGINE: "sync"
    BS_LIST: "4k 512k"
    SKIP_INSTALL: "False"

run:
    steps:
        - cd ./automated/linux/fio-test/
        - ./fio-test.sh -p "${PARTITION}" -i "${IOENGINE}" -b "${BS_LIST}" -s "${SKIP_INSTALL}"
        - ../../utils/send-to-lava.sh ./output/result.txt
