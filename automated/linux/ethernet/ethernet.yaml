metadata:
    name: ethernet
    format: "Lava-Test-Shell Test Definition 1.0"
    description: "This test checks if Ethernet is up and prints IP address."
    maintainer:
        - naresh.kamboju@linaro.org
    os:
        - debian
        - ubuntu
        - centos
        - fedora
    scope:
        - functional
    devices:
        - panda
        - panda-es
        - arndale
        - vexpress-tc2
        - beaglebone-black
        - juno
        - hi6220-hikey
        - apq8016-sbc

params:
    INTERFACE: "eth0"

run:
    steps:
        - cd automated/linux/ethernet
        - ./ethernet.sh -i "${INTERFACE}"
        - ../../utils/send-to-lava.sh ./output/result.txt
