metadata:
    format: Lava-Test Test Definition 1.0
    name: wlan-smoke
    description: "Basic WLAN smoke tests"
    maintainer:
        - nicolas.dechesne@linaro.org
    os:
        - debian
        - ubuntu
        - centos
        - fedora
    scope:
        - functional
    devices:
        - dragonboard410c

params:
    # leave empty to auto-detect and test all wireless interfaces present
    DEVICE: ""
    # expected state on first boot: <enabled> or <disabled>
    BOOT: enabled
    WAIT_TIME: 0

run:
    steps:
        - cd ./automated/linux/wlan-smoke
        - ./wlan-smoke-test.sh -d "${DEVICE}" -b "${BOOT}" -w "${WAIT_TIME}"
        - ../../utils/send-to-lava.sh ./output/result.txt
