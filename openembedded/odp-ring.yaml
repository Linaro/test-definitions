metadata:
    name: odp_ring
    version: "1.0"
    format: "Lava-Test-Shell Test Definition 1.0"
    description: "Run odp-ring"
    maintainer:
        - mike.holmes@linaro.org
    os:
        - openembedded
    devices:
        - arndale
    environment:
        - lava-test-shell

run:
    steps:
        - lava-test-case odp-ring --shell  /usr/share/odp/odp_ring
