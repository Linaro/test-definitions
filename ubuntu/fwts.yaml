metadata:
    name: fwts
    format: "Lava-Test-Shell Test Definition 1.0"
    description: "Firmware Test Suite (fwts) on ubuntu."

install:
    deps:
        - fwts
run:
    steps:
        - 'sudo fwts -a'
