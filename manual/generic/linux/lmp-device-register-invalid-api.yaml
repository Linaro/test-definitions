metadata:
    name: lmp-device-register-invalid-api
    format: "Manual Test Definition 1.0"
    description: "Register device via invalid api token"
    maintainer:
        - brendan.wood@foundires.io
    os:
        - ubuntu
        - openembedded
    scope:
        - functional
    devices:
        - 
    environment:
        - manual-test

run:
    steps:
        - boot up the device
        - ensure the device is not currently registered to a factory
        - run "lmp-device-register -T<invalid-api-token> -n<name>"         
                       
    expected:
       - Device should not register
       - expected error - HTTP_401 Polis Error
